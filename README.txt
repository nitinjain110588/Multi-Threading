NITIN JAIN
CS-402 Fall 2013
STUDENT ID- 8008168591

Warmup2

Emulation of token bucket filter. Created three threads: one for packet generation, one for token generation, one for servicing the packets.

Emulation runs in two modes: one is deterministic mode, another is trace file mode.

For compilation: make
